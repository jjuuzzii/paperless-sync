# Gemeinsame Backend-Schicht (config_manager, matcher, exporter,
# paperless_client, session_store, csv_utils) - fuer alle UI-Varianten noetig.
pandas>=2.0,<3.0
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0
python-dateutil>=2.8,<3.0
requests-pkcs12>=1.20,<2.0

# Aktuelle Desktop-UI (PySide6/Qt, siehe desktop_app_qt.py und build.py).
PySide6>=6.5,<7.0

# Aeltere CustomTkinter-Desktop-UI (desktop_app.py) - durch die Qt-Variante
# abgeloest (siehe build.py-Docstring), nur noch als Fallback nutzbar.
customtkinter>=5.2,<7.0
tkinterdnd2>=0.3,<1.0

# Nur noetig fuer die alte Streamlit-Weboberflaeche (app.py, siehe
# build_streamlit_legacy.py) - fuer die aktuelle Desktop-App nicht erforderlich.
streamlit>=1.32,<2.0
